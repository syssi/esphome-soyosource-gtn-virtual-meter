substitutions:
  name: pm-tasmota-http-sns

esphome:
  name: ${name}
  friendly_name: ${name}

esp8266:
  board: d1_mini

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  min_auth_mode: WPA2

ota:
  platform: esphome

logger:
  level: DEBUG

packages:
  powermeter: !include ../powermeter-sensor-examples/tasmota-http-status-sns.yaml
