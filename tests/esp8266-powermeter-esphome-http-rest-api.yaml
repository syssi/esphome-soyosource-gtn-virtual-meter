substitutions:
  name: pm-esphome-http-api

esphome:
  name: ${name}

esp8266:
  board: d1_mini

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

ota:
  platform: esphome

logger:
  level: DEBUG

packages:
  powermeter: !include ../powermeter-sensor-examples/esphome-http-rest-api.yaml
